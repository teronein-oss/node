# NODE 회원가입 전환 상태

현재 운영 중인 Google 계정 로그인은 기존 사용자를 위해 유지한다. 기존의 Google 가입 신청, 학원 코드 입력, 역할 직접 선택 경로는 화면과 클라이언트 API에서 제거했다. 신규 가입은 아직 열리지 않는다.

## 신규 계정 흐름

1. 이용약관과 개인정보 처리 안내의 **게시된 버전**을 보여주고 필수 동의를 받는다. 이메일·문자 마케팅 수신은 각각 선택 동의로 기록한다.
2. 계약된 휴대폰 본인확인 제공사의 결과를 서버가 검증한다. 통신사 SMS/PASS 등 제공사의 본인확인 결과가 필요하며 Firebase 전화 OTP나 문자 발송 성공만으로 실명인증으로 간주하지 않는다.
3. 본인확인 결과에서 이름, 휴대폰, 중복가입 식별자를 받는다. 제공사 콜백은 10분 이내 만료되는 무작위 비밀 티켓을 생성하며 Firestore `identityVerificationTickets/{SHA256(ticket)}`에 검증 상태와 만료 시각을 서버 권한으로만 저장한다. `subjectHash`는 제공사의 중복가입 식별자를 서버 비밀키로 HMAC 처리한 값으로 생성해야 한다. 원본 식별자는 클라이언트 또는 일반 회원 문서에 저장하지 않는다.
4. 이용자가 이메일과 비밀번호를 설정해 Firebase 이메일·비밀번호 계정을 생성한다. Auth 토큰의 로그인 제공사가 `password`인지 서버에서 확인한다.
5. `registerNodeAccount`가 티켓, 동의 버전, 가입 유형을 검증하고 한 트랜잭션에서 워크스페이스, `pending_email` 등록 정보, 중복가입 키를 만든다. 개인 강사는 본인 이름의 개인 공간과 `선생님` 역할을, 학원 원장은 학원 공간과 `원장` 역할을 받는다.
6. 이메일 확인 링크가 검증되면 `activateNodeAccount`가 등록 상태를 `approved`로 바꾼다. 이 전에는 대시보드에 접근할 수 없다.

## 현재 필요한 외부 준비

- 휴대폰 본인확인 제공사 계약과 서버 콜백 연동. 현재 계약된 제공사가 없어 티켓을 발급할 경로가 없다. 임의 티켓이나 일반 SMS 인증으로 우회하지 않는다.
- 게시할 서비스 이용약관, 개인정보 처리 안내, 보유 기간, 마케팅 선택 동의 문안과 버전 확정. 문안 없이 `signupConfig/current.enabled`를 켜지 않는다.
- 제공사 연동 후 프런트엔드에 실제 약관 보기, 본인확인 진입, 이메일·비밀번호 입력, 중복 이메일 안내, 이메일 확인 화면을 연결한다. Firebase Console에서 이메일·비밀번호 로그인 제공자 활성화와 발신 도메인/이메일 템플릿도 확인한다.
- 원장→강사, 강사→조교 초대는 별도 일회용 초대 토큰과 소속/담당 강사 연결을 구현해야 한다. 초대받은 사용자도 같은 본인확인·약관·이메일 검증을 거친다. 현재 UI에는 가입 유형 안내만 있고 초대 수락 경로는 열리지 않는다.

현재 `signupConfig/current`가 없거나 비활성화된 경우 서버가 가입을 거부하고 화면도 가입 완료를 허용하지 않는다. 운영 배포 전 Firestore 규칙 컴파일과 실제 제공사 샌드박스에서 정상·만료·재사용·중복가입·이메일 미인증 시나리오를 검증한다.
